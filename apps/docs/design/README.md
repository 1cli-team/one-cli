# Historical design reference

`design.pen` is an archived visual prototype, not the current product specification.
It includes retired agent-skill installation and generated AI instructions.
Do not restore those features or reuse their copy from this file.

The live website is implemented under `apps/docs/src/`; current CLI behavior is
defined by its command implementations and verified help output. This prototype
is not an input to the website or CLI build.
