
DESCRIPTION
container/dockerhub

USAGE
  one configure add container/dockerhub [--profile <name>] [flags]

TIPS
新增或更新 container/dockerhub profile（Docker Hub）。

host 固定为 index.docker.io —— 不需要 --registry。namespace 留空时
默认等于 username（Docker Hub 的个人 / 组织目录通常等于账号名）。

凭据建议用 Docker Hub 的 Personal Access Token（Account Settings →
Security → Personal access tokens）而不是登录密码。

示例：
  # 交互（推荐）
  one configure add container/dockerhub

  # 非交互（CI / 脚本）
  one configure add container/dockerhub --profile prod \
      --username $DOCKER_HUB_USER \
      --password $DOCKER_HUB_PAT \
      --namespace my-org --use

COMMON OPTIONS
  --namespace <value>      Docker Hub namespace（个人账号名 / 组织名；空则默认等于 username）
  -o, --output <value>     Output format: json | yaml | text (default: terminal-friendly text, JSON when piped)
  --password <value>       Docker Hub access token (PAT)
  --use                    Make this the current local connection
  --username <value>       Docker Hub username

AUTOMATION AND ADVANCED OPTIONS
  --profile <value>        Local-connection name (required in non-interactive calls)
