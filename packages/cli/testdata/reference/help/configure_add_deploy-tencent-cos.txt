
DESCRIPTION
deploy/tencent-cos

USAGE
  one configure add deploy/tencent-cos [--profile <name>] [flags]

TIPS
新增或更新 deploy/tencent-cos profile。

腾讯云 COS（S3 协议）。所有 S3-protocol 兼容后端共享同一 profile 结构（endpoint + region +
AccessKey 对）；六个 backend id 只是给同一段实现起的不同名字，方便区分
你的 endpoint 来自哪家供应商。

bucket 是 subproject 级字段，由 `one add` 时的 prompt 写入
projects[i].deploy.bucket，不存在 profile 里。

示例：
  # 交互（推荐）
  one configure add deploy/tencent-cos

  # 非交互（CI / 脚本）
  one configure add deploy/tencent-cos --profile prod \
      --endpoint https://cos.<region>.myqcloud.com \
      --region <region>（如 ap-guangzhou / ap-shanghai） \
      --access-key-id $AK --access-key-secret $SK --use

通常不需要 --force-path-style。

COMMON OPTIONS
  --access-key-id <value>  AccessKey ID
  --access-key-secret <value> AccessKey Secret
  --endpoint <value>       S3 endpoint URL（如 https://cos.<region>.myqcloud.com）
  --force-path-style       用 path-style 寻址（MinIO / RustFS 默认开启；AWS / Aliyun / 腾讯 / R2 通常关闭）
  -o, --output <value>     Output format: json | yaml | text (default: terminal-friendly text, JSON when piped)
  --region <value>         Region（如 ap-guangzhou / ap-shanghai）
  --use                    Make this the current local connection

AUTOMATION AND ADVANCED OPTIONS
  --profile <value>        Local-connection name (required in non-interactive calls)
