
DESCRIPTION
deploy/tencent-cos

USAGE
  one configure add deploy/tencent-cos [--profile <name>] [flags]

TIPS
新增或更新 deploy/tencent-cos Profile。

字段由 Backend Catalog 提供：
  --endpoint  可选
  --region  可选
  --force-path-style  可选
  --access-key-id  必填
  --access-key-secret  必填

示例：
  one configure add deploy/tencent-cos --profile prod --endpoint https://cos.<region>.myqcloud.com --region ap-guangzhou --access-key-id <access-key-id> --access-key-secret <access-key-secret>

第一次创建会自动成为 default；同名调用会更新，--use 会显式切换 default。

COMMON OPTIONS
  --access-key-id <value>  access key id（必填）
  --access-key-secret <value> access key secret（必填）
  --endpoint <value>       endpoint（如 https://cos.<region>.myqcloud.com）
  --force-path-style       force path style
  -o, --output <value>     Output format: json | yaml | text (default: terminal-friendly text, JSON when piped)
  --region <value>         region（如 ap-guangzhou）
  --use                    Make this the current local connection

AUTOMATION AND ADVANCED OPTIONS
  --profile <value>        Local-connection name (required in non-interactive calls)
