
DESCRIPTION
container/acr

USAGE
  one configure add container/acr [--profile <name>] [flags]

TIPS
新增或更新 container/acr Profile。

字段由 Backend Catalog 提供：
  --region  必填
  --username  必填
  --namespace  可选
  --password  必填

示例：
  one configure add container/acr --profile prod --region cn-hangzhou --username <username> --namespace <namespace> --password <password>

第一次创建会自动成为 default；同名调用会更新，--use 会显式切换 default。

COMMON OPTIONS
  --namespace <value>      namespace
  -o, --output <value>     Output format: json | yaml | text (default: terminal-friendly text, JSON when piped)
  --password <value>       password（必填）
  --region <value>         region（如 cn-hangzhou）（必填）
  --use                    Make this the current local connection
  --username <value>       username（必填）

AUTOMATION AND ADVANCED OPTIONS
  --profile <value>        Local-connection name (required in non-interactive calls)
