
DESCRIPTION
deploy/kustomize

USAGE
  one configure add deploy/kustomize [--profile <name>] [flags]

TIPS
新增或更新 deploy/kustomize Profile。

字段由 Backend Catalog 提供：
  --kubeconfig  可选
  --kubeconfig-context  可选

示例：
  one configure add deploy/kustomize --profile prod-k8s --kubeconfig ~/.kube/config --kubeconfig-context <kubeconfig-context>

第一次创建会自动成为 default；同名调用会更新，--use 会显式切换 default。

COMMON OPTIONS
  --kubeconfig <value>     kubeconfig（如 ~/.kube/config）
  --kubeconfig-context <value> kubeconfig context
  -o, --output <value>     Output format: json | yaml | text (default: terminal-friendly text, JSON when piped)
  --use                    Make this the current local connection

AUTOMATION AND ADVANCED OPTIONS
  --profile <value>        Local-connection name (required in non-interactive calls)
